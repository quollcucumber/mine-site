{
  "name": "mine-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && wrangler dev --port 8787",
    "build": "npm run web:build",
    "cf:dev": "npm run build && wrangler dev --port 8787",
    "cf:deploy": "npm run build && wrangler deploy",
    "test": "node test-vector.mjs && node bridge-test.mjs && node store-test.mjs",
    "lint": "tsc --noEmit -p web/tsconfig.json",
    "web:build": "vite build --config web/vite.config.ts"
  },
  "dependencies": {
    "randomx.js": "^0.1.3"
  },
  "devDependencies": {
    "@types/node": "^22.15.21",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "wrangler": "4.80.0"
  }
}
